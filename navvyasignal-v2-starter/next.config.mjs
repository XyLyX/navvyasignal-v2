/** Temporary static-export mode for the initial V2 preview only.
 * Do not enable server-only Notion content until the Netlify Next.js runtime is configured.
 */
const nextConfig = {
  output: 'export',
};

export default nextConfig;
