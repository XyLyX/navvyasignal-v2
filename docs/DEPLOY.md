# Preview-only deployment

Connect this private repository to the Netlify project `navvyasignal-v2-preview` **only** under Netlify account `navvyasignal@outlook.com` (team `navvyasignal`). Leave `navvyasignal.com` and Framer V1 untouched. Read-only Notion credentials are optional for the initial empty preview; never use `Synced to Framer` as a V2 cursor.
